#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

#define rep(i, n) for (int i = 0; i < (int)n; i++)

int main()
{
	int n, m; ll p; cin >> n >> m >> p; int array[n];
	
	rep(i, m){
		ll l, r, k; cin >> l >> r >> k;
	}

	return 0;
}